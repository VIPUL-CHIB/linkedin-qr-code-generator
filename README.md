# linkedin-qr-code-generator
A Python project that generates a customized QR code linking to a LinkedIn profile using the qrcode and Pillow libraries. The QR code is styled with a red foreground and white background, saved as a PNG image, and demonstrates how to add data, fit the QR code, and customize its appearance.
